A Single Page Application (SPA) built with React and TypeScript to display Rick and Morty characters using the public API.

Features include character listing, pagination, and data fetching with React Query.
